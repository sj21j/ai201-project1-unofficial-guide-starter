sentence-transformers==3.4.1
chromadb>=0.6.0
pysqlite3-binary>=0.5.0  # ChromaDB needs sqlite3>=3.35.0; bundles a newer build
groq==0.15.0
python-dotenv==1.0.1

# Query interface (Milestone 5)
gradio>=6.9.0
huggingface-hub<1.0,>=0.34.0  # pin <1.0 for transformers/sentence-transformers compatibility
# streamlit>=1.40.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
